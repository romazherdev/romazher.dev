---
interface Props {
  anchorId: string;
}

const { anchorId } = Astro.props;
---

<a
  id={anchorId}
  class="absolute -left-full top-0 focus:left-0 focus:right-0 z-20 text-center p-1 bg-primary text-background w-44 max-w-full mx-auto"
  href="#main">Skip navigation</a
>
